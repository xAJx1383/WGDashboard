---
phase: 01-stabilization
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/modules/WireguardConfiguration.py, src/dashboard.py, src/tests/test_stab_track.py]
autonomous: true
requirements: [STAB-01, STAB-02, STAB-04]
must_haves:
  truths:
    - "Peer updates use 'wg set' and 'wg-quick save' without interface restarts"
    - "Application shutdown flushes pending usage data to the database"
  artifacts:
    - path: "src/dashboard.py"
      provides: "atexit flush handler"
  key_links:
    - from: "src/modules/WireguardConfiguration.py"
      to: "wg set"
      via: "WireguardCLI call"
---

<objective>
Implement atomic configuration management and a graceful shutdown handler to ensure session stability and data persistence.

Purpose: Prevent peer disconnects during configuration changes and avoid usage data loss on service termination.
Output: Refactored peer management methods and a new shutdown flush routine.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/01-02-SUMMARY.md
@src/modules/WireguardConfiguration.py
@src/dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Atomic Peer Management</name>
  <files>src/modules/WireguardConfiguration.py, src/tests/test_stab_track.py</files>
  <action>
    1. Refactor `WireguardConfiguration.addPeer`, `removePeer`, and any `Peer.updatePeer` logic to use `wg set` for live changes.
    2. Follow every `wg set` call with `wg-quick save <interface>` to persist the change to the disk-based configuration.
    3. Ensure no `wg-quick down/up` calls are made during these operations.
    4. Add integration tests to `src/tests/test_stab_track.py` to verify that these commands are called in sequence and the interface remains up.
  </action>
  <verify>
    <automated>python3 -c "import sys; content = open('src/modules/WireguardConfiguration.py').read(); has_atomic = 'set' in content and 'save' in content and 'down' not in content; sys.exit(0 if has_atomic else 1)"</automated>
  </verify>
  <done>Peer management is atomic and persistent without session interruption.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Graceful Shutdown Flush</name>
  <files>src/dashboard.py, src/tests/test_stab_track.py</files>
  <action>
    1. Implement a `flush_usage_on_shutdown()` function in `src/dashboard.py`.
    2. The function should iterate through all active `WireguardConfiguration` objects and call `updatePeersData()`.
    3. Register this function using the `atexit` module.
    4. Add a test case in `src/tests/test_stab_track.py` that mocks `atexit` and verifies `updatePeersData` is called for all configurations.
  </action>
  <verify>
    <automated>pytest src/tests/test_stab_track.py -k "test_graceful_shutdown"</automated>
  </verify>
  <done>Service reliably flushes all pending usage data to the database before exiting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service -> OS | OS signals (SIGTERM/SIGINT) trigger the shutdown sequence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | Network Config | mitigate | Atomic updates eliminate the window of downtime caused by full interface restarts. |
| T-01-05 | Information Loss | Usage Tracking | mitigate | `atexit` handler ensures final usage deltas are persisted even on unplanned (but graceful) shutdowns. |
</threat_model>

<verification>
- Verify `wg set` and `wg-quick save` pattern via automated script.
- Verify `pytest` confirms the shutdown flush logic works under mock conditions.
</verification>

<success_criteria>
- Zero downtime for existing peers during management operations.
- All usage data persisted to DB on exit.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-03-SUMMARY.md`
</output>
