---
phase: 01-stabilization
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/modules/WireguardConfiguration.py, src/modules/Peer.py, src/dashboard.py]
autonomous: true
requirements: [STAB-01, STAB-02, STAB-04]
must_haves:
  truths:
    - "Peer updates (allowed-ips, preshared-key) do not trigger interface restarts"
    - "Manual changes to live state are persisted to .conf files automatically"
    - "Application shutdown flushes the latest usage metrics to the database"
  artifacts:
    - path: "src/dashboard.py"
      provides: "atexit flush handler"
  key_links:
    - from: "src/modules/WireguardConfiguration.py"
      to: "wg set"
      via: "WireguardCLI call"
    - from: "src/modules/WireguardConfiguration.py"
      to: "wg-quick save"
      via: "WireguardCLI call"
---

<objective>
Implement atomic configuration updates and graceful shutdown handlers to ensure system stability and data integrity.

Purpose: Prevent session drops during peer management and avoid data loss on service termination.
Output: Refactored peer management methods and a new shutdown flush routine.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/01-02-SUMMARY.md
@src/modules/WireguardConfiguration.py
@src/modules/Peer.py
@src/dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Atomic Peer Updates</name>
  <files>src/modules/WireguardConfiguration.py, src/modules/Peer.py</files>
  <action>
    1. Refactor `Peer.updatePeer` and `WireguardConfiguration.removePeer` (and `addPeer`) to use `wg set` for live changes.
    2. Immediately follow every `wg set` call with `wg-quick save <interface>` to persist the change to the configuration file.
    3. Ensure no `wg-quick down/up` calls are made during these operations.
    4. Update error handling to revert database changes if CLI operations fail.
  </action>
  <verify>
    <automated># Manual verification: check if 'wg show' reflects changes without the interface going 'down' (e.g. ping remains stable)</automated>
  </verify>
  <done>Peer management is atomic and persistent without session interruption.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Graceful Shutdown Flush</name>
  <files>src/dashboard.py</files>
  <action>
    1. Implement a `flushAllUsage()` method in `src/dashboard.py` that iterates through all `WireguardConfigurations` and calls `updatePeersData()`.
    2. Register this method using the `atexit` module.
    3. Ensure the background tracking thread recognizes the `_app_stop_event` and completes its last cycle before the process exits.
  </action>
  <verify>
    <automated># Verification: Kill the process and check if the database reflects the most recent 'wg show' values</automated>
  </verify>
  <done>Service flushes all usage data to the database before exiting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service -> OS | Shutdown signals (SIGTERM/SIGINT) trigger the flush handler. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | Config Management | mitigate | Atomic updates prevent temporary service outages during peer management. |
</threat_model>

<verification>
- Verify `wg-quick down` is NOT called during a peer update.
- Verify `wg-quick save` correctly updates the `.conf` file on disk.
- Verify usage data is saved when the dashboard is stopped.
</verification>

<success_criteria>
- Zero-downtime peer updates.
- Automatic configuration persistence.
- Reliable data flushing on exit.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization/01-03-SUMMARY.md`
</output>
