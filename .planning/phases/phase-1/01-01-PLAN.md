---
phase: 01-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/modules/WireguardCLI.py, src/modules/WireguardConfiguration.py, src/modules/AmneziaWireguardConfiguration.py, src/modules/Peer.py, src/modules/AmneziaWGPeer.py, src/modules/Utilities.py, src/modules/SystemStatus.py, src/tests/test_delta_pattern.py, src/tests/test_cli_locking.py]
autonomous: true
requirements: [STAB-03]
must_haves:
  truths:
    - "Test directory and base test files exist"
    - "WireguardCLI wrapper provides serialized execution via global lock"
  artifacts:
    - path: "src/modules/WireguardCLI.py"
      provides: "Thread-safe WireGuard CLI execution"
    - path: "src/tests/test_delta_pattern.py"
      provides: "Unit tests for tracking logic"
    - path: "src/tests/test_cli_locking.py"
      provides: "Concurrency tests for WireguardCLI"
---

<objective>
Establish the stabilization foundation by implementing a thread-safe CLI wrapper and initializing the test suite with concurrency validation.

Purpose: Prevent race conditions in WireGuard interactions and enable automated verification of subsequent changes.
Output: WireguardCLI module and test scaffolds.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/VALIDATION.md
@src/modules/WireguardConfiguration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Test Suite and Concurrency Logic</name>
  <files>src/tests/test_delta_pattern.py, src/tests/test_cli_locking.py</files>
  <action>
    1. Create `src/tests/` directory if it doesn't exist.
    2. Create `src/tests/test_delta_pattern.py` with basic pytest structure (mocking peer data and testing delta calculations).
    3. Create `src/tests/test_cli_locking.py`. 
    4. Implement concurrency test logic in `test_cli_locking.py`: 
       - Use `threading` to spawn multiple threads (at least 10).
       - Each thread calls a mocked `WireguardCLI.run` that sleeps for a short duration.
       - Use a shared list to record the start and end times of each call.
       - Verify that no two calls overlap in time (end_time_i <= start_time_i+1 after sorting by start time), proving the lock is effective.
    5. Ensure tests are discoverable by `pytest`.
  </action>
  <verify>
    <automated>pytest src/tests/test_delta_pattern.py src/tests/test_cli_locking.py --collect-only</automated>
  </verify>
  <done>Test scaffolds and concurrency validation logic created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Thread-Safe CLI Wrapper and Refactor Callers</name>
  <files>src/modules/WireguardCLI.py, src/modules/WireguardConfiguration.py, src/modules/AmneziaWireguardConfiguration.py, src/modules/Peer.py, src/modules/AmneziaWGPeer.py, src/modules/Utilities.py, src/modules/SystemStatus.py</files>
  <action>
    1. Create `src/modules/WireguardCLI.py` with a `WireguardCLI` class containing a static `threading.Lock`.
    2. Implement `run(command_list, timeout=10)` method that executes `subprocess.check_output` within the lock's context.
    3. Refactor all identified callers to import `WireguardCLI` and replace direct `subprocess.check_output` (or `run`, `Popen`, `call`) calls with `WireguardCLI.run()`:
       - `src/modules/WireguardConfiguration.py`
       - `src/modules/AmneziaWireguardConfiguration.py`
       - `src/modules/Peer.py`
       - `src/modules/AmneziaWGPeer.py`
       - `src/modules/Utilities.py` (specifically for `wg pubkey` and `genkey`)
       - `src/modules/SystemStatus.py` (specifically for `ip route show`)
    4. Ensure the wrapper handles `subprocess.CalledProcessError` and `subprocess.TimeoutExpired` correctly.
  </action>
  <verify>
    <automated>python3 -c "import sys, glob; targets = ['src/modules/WireguardConfiguration.py', 'src/modules/AmneziaWireguardConfiguration.py', 'src/modules/Peer.py', 'src/modules/AmneziaWGPeer.py', 'src/modules/Utilities.py', 'src/modules/SystemStatus.py']; results = []; [results.append('subprocess.' in open(f).read() and 'WireguardCLI.run' not in open(f).read()) for f in targets]; sys.exit(1 if any(results) else 0)"</automated>
  </verify>
  <done>All system CLI calls are now routed through the thread-safe wrapper, and direct subprocess usage is removed from core modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> OS | CLI commands execute with sudo/root privileges. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Config Files | mitigate | Serialize CLI calls via `WireguardCLI` lock to prevent overlapping writes/reads that could corrupt live state. |
| T-01-02 | Denial of Service | System Resources | mitigate | Implement strict timeouts in `WireguardCLI.run` to prevent zombie processes from hanging the application. |
</threat_model>

<verification>
- Verify `pytest` can find and collect the new test files.
- Verify `pytest src/tests/test_cli_locking.py` passes.
- Verify that core modules no longer contain direct `subprocess` calls (per Task 2 verification script).
</verification>

<success_criteria>
- Test environment initialized with concurrency validation.
- Global lock implemented and verified for all system CLI interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-01-SUMMARY.md`
</output>
