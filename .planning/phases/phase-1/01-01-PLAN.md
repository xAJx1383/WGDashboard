---
phase: 01-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/modules/WireguardCLI.py, src/modules/WireguardConfiguration.py, src/modules/AmneziaWireguardConfiguration.py]
autonomous: true
requirements: [STAB-03, TRACK-02]
must_haves:
  truths:
    - "All WireGuard CLI calls are serialized through a global lock"
    - "Database columns for traffic (total_receive, total_sent, etc.) use 64-bit integers"
  artifacts:
    - path: "src/modules/WireguardCLI.py"
      provides: "Thread-safe WireGuard CLI execution"
    - path: "src/modules/WireguardConfiguration.py"
      provides: "Updated database schema and CLI usage"
  key_links:
    - from: "src/modules/WireguardConfiguration.py"
      to: "src/modules/WireguardCLI.py"
      via: "import and method calls"
---

<objective>
Establish the technical foundation for robust tracking and stabilization by implementing thread-safe CLI interactions and high-precision storage.

Purpose: Prevent race conditions during concurrent WireGuard operations and enable byte-perfect tracking.
Output: A new CLI wrapper module and updated database schemas.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@src/modules/WireguardConfiguration.py
@src/modules/AmneziaWireguardConfiguration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Thread-Safe WireGuard CLI Wrapper</name>
  <files>src/modules/WireguardCLI.py, src/modules/WireguardConfiguration.py</files>
  <action>
    1. Create `src/modules/WireguardCLI.py` implementing a class with a global `threading.Lock`.
    2. Provide a method `run(cmd, timeout=10)` that wraps `subprocess.check_output` within the lock.
    3. Refactor `WireguardConfiguration.py` to import and use this wrapper for all calls currently using `subprocess.check_output` with "wg" or "wg-quick".
    4. Ensure the wrapper captures and logs CLI errors consistently.
  </action>
  <verify>
    <automated>grep -r "subprocess.check_output" src/modules/WireguardConfiguration.py | grep -v "WireguardCLI" | wc -l # Should be 0 for wg/wg-quick calls</automated>
  </verify>
  <done>WireguardCLI module created and integrated into WireguardConfiguration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Storage to BigInteger</name>
  <files>src/modules/WireguardConfiguration.py, src/modules/AmneziaWireguardConfiguration.py</files>
  <behavior>
    - When creating a new configuration database, traffic columns (total_receive, total_sent, total_data, cumu_receive, cumu_sent, cumu_data) must be of type BigInteger.
    - Existing float-based logic for these columns must be removed in favor of raw integer storage.
  </behavior>
  <action>
    1. In `WireguardConfiguration.createDatabase`, change `sqlalchemy.Float` to `sqlalchemy.BigInteger` for all traffic-related columns.
    2. Repeat for `AmneziaWireguardConfiguration.createDatabase`.
    3. Note: Since SQLite integers are 64-bit by default, this change ensures SQLAlchemy treats them as such across all supported backends (PostgreSQL, MySQL).
  </action>
  <verify>
    <automated>python3 -c "from src.modules.WireguardConfiguration import WireguardConfiguration; import sqlalchemy; print('BigInteger' in str(WireguardConfiguration.createDatabase))" # Manual check of source or run a test script</automated>
  </verify>
  <done>Database schemas updated to use BigInteger for all usage metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App -> OS | CLI commands are executed with elevated privileges (root). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | WireGuard CLI | mitigate | Use `WireguardCLI` wrapper to ensure serialized execution and prevent race-condition-induced config corruption. |
| T-01-02 | Denial of Service | System Calls | mitigate | Implement strict timeouts (10s) in `WireguardCLI` to prevent hung CLI processes from locking the entire app. |
</threat_model>

<verification>
- Verify that a new database created using the updated code has `BIGINT` columns.
- Verify that concurrent calls to the CLI are serialized (can be tested with a small script that spawns threads).
</verification>

<success_criteria>
- No more floating point types used for usage metrics in the database schema.
- All WireGuard CLI interactions are protected by the global lock.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization/01-01-SUMMARY.md`
</output>
