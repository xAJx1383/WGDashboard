---
phase: 01-stabilization
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/modules/WireguardConfiguration.py, src/modules/Peer.py, src/modules/Utilities.py]
autonomous: true
requirements: [TRACK-01, TRACK-03, TRACK-04]
must_haves:
  truths:
    - "Peer usage tracking uses raw bytes collected via 'wg show dump'"
    - "Counter resets are handled by adding the full current counter to the cumulative total"
    - "Usage data is formatted only at the edges (Utilities or Frontend)"
  artifacts:
    - path: "src/modules/Utilities.py"
      provides: "FormatBytes helper"
  key_links:
    - from: "src/modules/WireguardConfiguration.py"
      to: "wg show dump"
      via: "WireguardCLI call"
---

<objective>
Implement high-precision traffic tracking using the Delta Pattern and raw byte collection.

Purpose: Eliminate precision loss caused by GB-based tracking and ensure accurate usage accounting across reboots/resets.
Output: Updated tracking loop and peer models handling raw integers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/01-01-SUMMARY.md
@src/modules/WireguardConfiguration.py
@src/modules/Peer.py
@src/modules/Utilities.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Raw Byte Delta Pattern</name>
  <files>src/modules/WireguardConfiguration.py</files>
  <behavior>
    - If current_rx < last_rx (reset), delta_rx = current_rx.
    - Else, delta_rx = current_rx - last_rx.
    - cumulative_rx += delta_rx.
    - Same logic for tx.
    - All calculations performed on raw integers.
  </behavior>
  <action>
    1. Refactor `updatePeersData` to remove all floating-point divisions (`1024**3`).
    2. Use `WireguardCLI` to fetch `wg show <interface> dump`.
    3. Implement the Delta Pattern as described in RESEARCH.md for raw byte integers.
    4. Ensure `total_receive` and `total_sent` in the database store the latest raw values from `wg show`.
    5. Update `cumu_receive` and `cumu_sent` using the calculated deltas.
  </action>
  <verify>
    <automated>pytest src/tests/test_delta_pattern.py # Agent must create this test</automated>
  </verify>
  <done>updatePeersData successfully tracks usage with byte-level precision.</done>
</task>

<task type="auto">
  <name>Task 2: Update Peer Model and Utilities</name>
  <files>src/modules/Peer.py, src/modules/Utilities.py</files>
  <action>
    1. Update `Peer.__init__` and `Peer.toJson` to treat usage fields as integers.
    2. Add `FormatBytes(bytes, precision=2)` to `src/modules/Utilities.py` for consistent formatting.
    3. Update `Peer.resetDataUsage` to ensure it sets integers to 0.
  </action>
  <verify>
    <automated>python3 -c "from src.modules.Utilities import FormatBytes; print(FormatBytes(1073741824))" # Should output "1.00 GB" or similar</automated>
  </verify>
  <done>Peer model handles raw bytes; formatting utility available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> App | Integrity of stored integers is critical for accurate billing/monitoring. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Peer Usage | mitigate | Ensure byte-precision tracking does not overflow (using BigInteger) to prevent incorrect usage display. |
</threat_model>

<verification>
- Verify tracking remains accurate after simulated `wg-quick down/up` (simulated reset).
- Verify no precision loss when adding small amounts of traffic (e.g., 512 bytes) to a large total (e.g., 10 GB).
</verification>

<success_criteria>
- Usage tracking correctly handles counter resets.
- Usage data is stored and manipulated as raw integers in the backend.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization/01-02-SUMMARY.md`
</output>
