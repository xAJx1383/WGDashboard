---
phase: 01-stabilization
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/modules/WireguardConfiguration.py, src/modules/AmneziaWireguardConfiguration.py, src/modules/Peer.py, src/modules/Utilities.py, src/tests/test_delta_pattern.py, src/tests/test_stab_track.py]
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03, TRACK-04]
must_haves:
  truths:
    - "Existing GB float data is successfully migrated to bytes"
    - "Database traffic columns use BigInteger for precision"
    - "Usage tracking logic correctly handles counter resets using raw bytes"
    - "Formatting utility exists for human-readable usage display"
  artifacts:
    - path: "src/modules/Utilities.py"
      provides: "FormatBytes helper"
  key_links:
    - from: "src/modules/WireguardConfiguration.py"
      to: "BigInteger columns"
      via: "SQLAlchemy schema definition"
---

<objective>
Implement high-precision traffic tracking by migrating existing data, updating the database schema to BigInteger, and refactoring the tracking logic to use raw bytes and the Delta Pattern.

Purpose: Eliminate precision loss from GB conversion and ensure accurate tracking across service/interface resets.
Output: Migrated database, high-precision schema, byte-aware tracking loop, and peer model.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/phase-1/01-01-SUMMARY.md
@src/modules/WireguardConfiguration.py
@src/modules/AmneziaWireguardConfiguration.py
@src/modules/Peer.py
@src/modules/Utilities.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data Migration and Atomic Schema Update</name>
  <files>src/modules/WireguardConfiguration.py, src/modules/AmneziaWireguardConfiguration.py, src/tests/test_stab_track.py</files>
  <behavior>
    - Existing traffic values in GB (float) must be converted to raw bytes (int) by multiplying by 1024**3.
    - traffic columns (total_receive, total_sent, total_data, cumu_receive, cumu_sent, cumu_data) must be sqlalchemy.BigInteger.
    - updatePeersData must store and manipulate raw bytes (integers), removing all floating-point divisions by 1024**3.
  </behavior>
  <action>
    1. Implement a data migration routine:
       - Before modifying the schema, query all records in the peer table.
       - Multiply `total_receive`, `total_sent`, etc., by `1073741824` (1024**3).
       - Commit these changes. 
       - *Note*: If using SQLite, this can be done via raw SQL `UPDATE peer SET total_receive = CAST(total_receive * 1073741824 AS INTEGER), ...`
    2. In `WireguardConfiguration.createDatabase` and `AmneziaWireguardConfiguration.createDatabase`, change all traffic column types from `sqlalchemy.Float` to `sqlalchemy.BigInteger`.
    3. Refactor `WireguardConfiguration.updatePeersData`:
        - Remove division by `(1024 ** 3)` when reading `transfer_rx/tx` from `wg show dump`.
        - Implement the Delta Pattern for raw integers:
          - If `current_rx < last_rx`, `delta = current_rx`.
          - Else, `delta = current_rx - last_rx`.
          - `cumulative_rx += delta`.
        - Ensure all database updates for these fields use raw integers.
    4. Update `src/tests/test_stab_track.py` to verify the schema change, the successful data migration, and tracking precision.
  </action>
  <verify>
    <automated>pytest src/tests/test_stab_track.py</automated>
  </verify>
  <done>Database migrated to bytes and tracking logic updated to high-precision raw bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Peer Model and Utilities</name>
  <files>src/modules/Peer.py, src/modules/Utilities.py, src/tests/test_delta_pattern.py</files>
  <action>
    1. Update `Peer` class (init and methods) to treat usage as integers. Ensure `toJson` returns raw integers (formatting happens in UI or specific API helpers).
    2. In `src/modules/Utilities.py`, implement `FormatBytes(bytes, precision=2)` which converts bytes to a human-readable string (KB, MB, GB, TB).
    3. Finalize `src/tests/test_delta_pattern.py` with the actual tracking logic extracted from `WireguardConfiguration.py` (or test the class method if possible).
  </action>
  <verify>
    <automated>pytest src/tests/test_delta_pattern.py</automated>
  </verify>
  <done>Peer model and utility functions support byte-level precision; logic verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> App | Integrity of cumulative usage integers is critical for accurate reporting. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Usage Stats | mitigate | Use BigInteger to prevent integer overflow for high-bandwidth peers, ensuring data integrity. |
</threat_model>

<verification>
- Verify tracking remains accurate (no data loss) after a simulated counter reset (current < last).
- Verify that small byte increments (e.g. 1024 bytes) are correctly added to large totals (e.g. 100 GB) without floating point rounding errors.
- Verify existing float data (e.g. 1.5 GB) is now stored as 1610612736 bytes in the database.
</verification>

<success_criteria>
- No floating point usage in tracking logic or database schema.
- Existing data successfully migrated to byte precision.
- Delta Pattern correctly accounts for WireGuard interface restarts.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-02-SUMMARY.md`
</output>
