---
phase: 02-restricted-peer-panel
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/peer_panel.py, static/dist/WGDashboardPeerPanel/panel.html]
autonomous: false
requirements: [PANEL-06]

must_haves:
  truths:
    - "Peers see a responsive web interface with their own connection statistics"
    - "Usage data (Upload, Download, Total) is displayed in human-readable format"
  artifacts:
    - path: "static/dist/WGDashboardPeerPanel/panel.html"
      provides: "Responsive peer panel UI"
  key_links:
    - from: "src/peer_panel.py"
      to: "static/dist/WGDashboardPeerPanel/panel.html"
      via: "render_template"
---

<objective>
Implement the responsive user interface for the restricted peer panel, allowing identified peers to view their connection metrics.

Purpose: Provide peers with a mobile-friendly view of their own stats (PANEL-06).
Output: A responsive HTML template and the associated Flask route.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-2/02-01-SUMMARY.md
@src/peer_panel.py
@src/modules/Utilities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create responsive Peer Panel template</name>
  <files>static/dist/WGDashboardPeerPanel/panel.html</files>
  <action>
    Create the directory `static/dist/WGDashboardPeerPanel/`.
    Create `panel.html` using Jinja2 and Bootstrap 5 (CDN).
    The template should display:
    - Peer Name and Public Key (ID).
    - Connection Status (Last Handshake).
    - Real-time Usage (Upload, Download, Total).
    - Allowed IPs assigned to the peer.
    - A "Refresh" button or meta-refresh for basic updates.
    Ensure the layout is responsive (Bootstrap cards/container).
  </action>
  <verify>
    <automated>test -f static/dist/WGDashboardPeerPanel/panel.html</automated>
  </verify>
  <done>HTML template exists with Bootstrap styling and peer data placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Implement usage route and data injection</name>
  <files>src/peer_panel.py</files>
  <action>
    Add a route `/usage` to the `peer_panel` Blueprint in `src/peer_panel.py`.
    - This route should use `flask.render_template('panel.html', peer=g.current_peer, format_bytes=FormatBytes)`.
    - Inject necessary utilities for byte formatting (use `src.modules.Utilities.FormatBytes`).
    - Ensure the route only serves the identified peer's data stored in `g.current_peer`.
  </action>
  <verify>
    <automated>pytest src/tests/test_peer_panel.py -k test_usage_route</automated>
  </verify>
  <done>Peer usage route is active and renders the identified peer's stats.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual and Responsive Check</name>
  <what-built>A responsive peer panel UI served via a read-only Flask route.</what-built>
  <how-to-verify>
    1. Register the blueprint in `src/dashboard.py` (temporary manual registration or wait for next plan).
    2. Mock a request from a known VPN IP.
    3. Visit the peer panel URL (e.g., http://localhost:10086/peer/usage).
    4. Resize browser to verify mobile responsiveness.
    5. Ensure no admin links or sensitive data from other peers are visible.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Client | Data displayed in the browser must be scoped to the identified peer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | UI Template | mitigate | Use server-side rendering (Jinja2) to ensure only matched peer data is sent to the client. |
</threat_model>

<success_criteria>
- Peer panel displays correct usage stats for the identified peer.
- UI is responsive and usable on mobile devices.
- No cross-peer data leakage is possible via the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-02-SUMMARY.md`
</output>
