---
phase: 02-restricted-peer-panel
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/modules/DashboardConfig.py, src/dashboard.py, src/peer_panel.py]
autonomous: true
requirements: [PANEL-05]

must_haves:
  truths:
    - "Admin can configure the peer panel port and binding interface"
    - "Peer panel runs on a secondary port if configured differently from the main dashboard"
    - "Peer panel is disabled by default"
  artifacts:
    - path: "src/modules/DashboardConfig.py"
      provides: "Configuration options for peer panel"
  key_links:
    - from: "src/dashboard.py"
      to: "src/peer_panel.py"
      via: "Blueprint registration and optional secondary server thread"
---

<objective>
Enable configurable binding for the restricted peer panel, supporting both same-port registration and independent secondary port execution (PANEL-05).

Purpose: Allow administrators to isolate the peer panel traffic to specific interfaces or ports.
Output: Configurable peer panel service integrated into the main dashboard runtime.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-2/02-02-SUMMARY.md
@src/dashboard.py
@src/modules/DashboardConfig.py
@src/peer_panel.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Peer Panel configuration items</name>
  <files>src/modules/DashboardConfig.py</files>
  <action>
    Update `src/modules/DashboardConfig.py`.
    Add `PeerPanel` section to `__default` dictionary:
    - `peer_panel_enable`: "false"
    - `peer_panel_port`: "10087"
    - `peer_panel_bind_address`: "0.0.0.0"
    
    Ensure these are saved to `wg-dashboard.ini` on initialization.
  </action>
  <verify>
    <automated>grep -i "peer_panel" src/modules/DashboardConfig.py</automated>
  </verify>
  <done>Configuration options for peer panel are available in the system.</done>
</task>

<task type="auto">
  <name>Task 2: Implement conditional Blueprint registration</name>
  <files>src/dashboard.py</files>
  <action>
    Update `src/dashboard.py`.
    1. Import `peer_panel` from `src/peer_panel.py`.
    2. Read `peer_panel_enable` and `peer_panel_port` from `DashboardConfig`.
    3. If `peer_panel_enable` is true:
       - If `peer_panel_port == app_port`: Register the blueprint on the main `app` object with `url_prefix='/peer'`.
       - If `peer_panel_port != app_port`: Do NOT register on main app; instead, prepare to start a secondary server.
  </action>
  <verify>
    <automated>python3 -c "from dashboard import app; print(app.blueprints)" | grep -q "peer_panel" || echo "Manual verify needed for conditional registration"</automated>
  </verify>
  <done>Peer panel is integrated into the Flask app structure.</done>
</task>

<task type="auto">
  <name>Task 3: Implement secondary port runner</name>
  <files>src/dashboard.py</files>
  <action>
    Implement `startPeerPanelThread()` in `src/dashboard.py`:
    - Create a new Flask app instance `peer_app`.
    - Set its `template_folder` and `static_folder` appropriately (referencing the peer panel dist).
    - Register the `peer_panel` blueprint on `peer_app`.
    - Define a `run_peer_app()` function that calls `peer_app.run(host=bind_address, port=port)`.
    - Start this as a daemon thread in `startThreads()` if `peer_panel_enable` is true and port is different.
    - Ensure it uses the same `WireguardConfigurations` global object.
  </action>
  <verify>
    <automated>pytest src/tests/test_peer_panel.py -k test_secondary_port_service</automated>
  </verify>
  <done>Peer panel can run on a separate port/interface as configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet -> Peer Panel Port | The peer panel port may be exposed independently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | Secondary Port | accept | A secondary service increases attack surface, but is required for isolation. Rationale: Low impact as service is read-only. |
</threat_model>

<success_criteria>
- If enabled and port matches main app, panel is accessible at `/peer/usage`.
- If enabled and port differs, panel is accessible at `http://[IP]:[PORT]/usage`.
- If disabled, panel is not accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-03-SUMMARY.md`
</output>
