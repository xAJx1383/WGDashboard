---
phase: 02-restricted-peer-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/peer_panel.py, src/tests/test_peer_panel.py]
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04]

must_haves:
  truths:
    - "System can resolve a WireGuard Peer object given an IP address matching its AllowedIPs"
    - "A dedicated Flask Blueprint exists that is isolated from admin routes"
    - "Requests from unknown IPs are rejected with 403 Forbidden"
  artifacts:
    - path: "src/peer_panel.py"
      provides: "Blueprint and IP identification logic"
    - path: "src/tests/test_peer_panel.py"
      provides: "Validation for IP matching and isolation"
  key_links:
    - from: "src/peer_panel.py"
      to: "src/modules/WireguardConfiguration.py"
      via: "iteration over Peers"
---

<objective>
Initialize the Restricted Peer Panel foundation by implementing the IP-based identification logic and the Flask Blueprint scaffold.

Purpose: Securely identify VPN peers without requiring login credentials, fulfilling PANEL-01 and PANEL-02.
Output: `src/peer_panel.py` containing identification logic and Blueprint definition.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/phase-2/RESEARCH.md
@src/dashboard.py
@src/modules/Utilities.py
@src/modules/WireguardConfiguration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement IP-to-Peer resolution logic</name>
  <files>src/peer_panel.py, src/tests/test_peer_panel.py</files>
  <behavior>
    - Test 1: Given an IP "10.0.0.2", it matches a peer with AllowedIPs "10.0.0.2/32".
    - Test 2: Given an IP "fd00::2", it matches a peer with AllowedIPs "10.0.0.2/32, fd00::2/128".
    - Test 3: Given an IP "10.0.0.5", it returns None if no peer matches.
    - Test 4: Handles overlapping subnets by returning the first active match.
  </behavior>
  <action>
    Create `src/peer_panel.py`. Implement `get_peer_by_ip(remote_addr, wireguard_configurations)`:
    - Iterate through `wireguard_configurations.values()`.
    - Only check active configurations (`getStatus()`).
    - For each peer, split `allowed_ip` by comma.
    - Use `src.modules.Utilities.IsIPInSubnet` to check if `remote_addr` matches any peer subnet.
    - Return the `Peer` object if found, else `None`.
    
    Create `src/tests/test_peer_panel.py` to verify this logic with mocked configuration objects.
  </action>
  <verify>
    <automated>pytest src/tests/test_peer_panel.py -k test_ip_identification</automated>
  </verify>
  <done>Peer resolution logic correctly identifies peers by their VPN internal IP.</done>
</task>

<task type="auto">
  <name>Task 2: Create Peer Panel Blueprint and Isolation Hook</name>
  <files>src/peer_panel.py</files>
  <action>
    Define a Flask Blueprint `peer_panel` in `src/peer_panel.py`.
    - Implement a `before_request` hook specifically for this blueprint.
    - The hook must use `get_peer_by_ip(request.remote_addr, ...)` to identify the peer.
    - If no peer is found, call `abort(403)`.
    - If found, store the peer object in `flask.g.current_peer` for route access.
    - Ensure all routes in this blueprint are read-only (GET only).
    - Add a test route `/usage_check` that returns `{"status": "identified", "peer_id": g.current_peer.id}`.
  </action>
  <verify>
    <automated>pytest src/tests/test_peer_panel.py -k test_blueprint_isolation</automated>
  </verify>
  <done>Blueprint exists with mandatory IP-based identification and 403 fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Peer -> Peer Panel | Untrusted network input (IP) used for identification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Identification | Mitigate | Rely on WireGuard kernel verification; only internal VPN IPs should reach the service. |
| T-02-02 | Information Disclosure | Peer Panel | Mitigate | Enforce `g.current_peer` isolation so peers cannot query other IDs. |
</threat_model>

<success_criteria>
- `get_peer_by_ip` passes all CIDR edge cases (v4/v6).
- Blueprint rejects non-VPN IPs with 403.
- Blueprint correctly identifies peers and makes their data available to routes.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-01-SUMMARY.md`
</output>
