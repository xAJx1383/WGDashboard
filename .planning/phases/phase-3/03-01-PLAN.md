---
phase: 03-integration-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/static/app/src/utilities/wireguard.js, src/static/app/src/components/configurationComponents/peerRow.vue, src/static/app/src/components/configurationComponents/peerDetailsModal.vue, src/static/app/src/components/configurationComponents/configurationCard.vue]
autonomous: true
requirements: [TRACK-05]
must_haves:
  truths:
    - "Frontend uses byte-level formatting instead of hardcoded GB scaling"
    - "Cumulative usage displays accurately in peer rows and configuration summaries"
  artifacts:
    - path: "src/static/app/src/utilities/wireguard.js"
      provides: "formatBytes utility"
---

<objective>
Update the Vue.js frontend to correctly display and format the high-precision byte-level data introduced in Phase 1.

Purpose: Ensure the UI matches the high-precision backend tracking.
Output: Updated Vue components and a shared formatting utility.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Byte Formatting Utility</name>
  <files>src/static/app/src/utilities/wireguard.js</files>
  <action>
    1. Check if `src/static/app/src/utilities/` exists.
    2. Implement `formatBytes(bytes, decimals = 2)` utility function that scales bytes to KB, MB, GB, TB.
    3. Export the function for use in Vue components.
  </action>
  <done>formatBytes utility implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Update Peer Rows and Summaries</name>
  <files>src/static/app/src/components/configurationComponents/peerRow.vue, src/static/app/src/components/configurationComponents/peerDetailsModal.vue, src/static/app/src/components/configurationComponents/configurationCard.vue</files>
  <action>
    1. Search for hardcoded usage display logic (e.g., `/ 1024**3`, `.toFixed(4) + " GB"`).
    2. Import `formatBytes` in these components.
    3. Replace legacy display logic with calls to `formatBytes(peer.cumu_data + peer.total_data)`.
    4. Ensure individual sent/received metrics also use the new formatter.
  </action>
  <done>All peer-related usage displays updated to use high-precision formatting.</done>
</task>

</tasks>

<verification>
- Verify that `formatBytes` handles various magnitudes (Bytes to TB) correctly.
- Inspect the modified Vue components to ensure all occurrences of legacy GB scaling are removed.
</verification>
